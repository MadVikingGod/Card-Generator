version: 2
updates:
  # Keeps the SHA-pinned actions in ci.yml from going stale, which is the
  # maintenance cost that comes with pinning them.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
