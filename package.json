{
  "name": "card-generator",
  "version": "1.0.0",
  "description": "A web-based trading card creator with a double-sided print layout.",
  "private": true,
  "scripts": {
    "lint": "eslint dist",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check"
  },
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "prettier": "^3.3.3"
  }
}
